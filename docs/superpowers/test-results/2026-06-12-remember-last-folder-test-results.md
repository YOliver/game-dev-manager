# Remember Last Folder - 测试结果

**测试人员：** oliveryin  
**日期：** 2026-06-12  
**应用版本：** 1.0.0  
**提交哈希：** a168044  

---

## 测试结果汇总

| 测试用例 | 通过 | 失败 | 备注 |
|-----------|------|------|-------|
| 1. 首次启动（无配置文件） | ✅ | | |
| 2. 选择文件夹保存配置 | ✅ | | |
| 3. 重启后自动恢复 | ✅ | | |
| 4. 无效路径处理 | ✅ | | |
| 5. 配置文件损坏处理 | ✅ | | |
| 6. 选择不同文件夹更新配置 | ✅ | | |
| 7. 权限错误处理（可选） | 跳过 | | Windows 上难以设置权限 |

**总体结果：** ✅ **通过** (6/6 通过，1 个跳过)

---

## 详细测试记录

### 测试用例 1：首次启动（无配置文件）

**目标：** 程序应启动并显示空白窗口，无错误。

**步骤：**
1. 确保配置文件不存在
2. 启动程序：`python -m gdm.main`
3. 观察主窗口

**预期结果：**
- ✅ 主窗口成功打开
- ✅ 无错误对话框或控制台错误
- ✅ 窗口标题："Game Dev Manager"
- ✅ 左侧面板（ProjectPanel）空白
- ✅ 中间面板（ThumbnailView）空白
- ✅ 右侧面板（DetailPanel）显示"无预览"

**实际结果：** 符合预期  
**通过 / 失败：** ✅ **通过**

---

### 测试用例 2：选择文件夹保存配置

**目标：** 选择文件夹后，配置文件应创建并包含 `last_folder` 路径。

**步骤：**
1. 启动程序（从测试用例 1）
2. 点击"文件" → "打开文件夹"
3. 选择包含图片的文件夹（例如 `C:\test_sprites`）
4. 验证中间面板显示缩略图
5. 关闭程序
6. 检查配置文件是否存在且内容正确

**预期结果：**
- ✅ 文件夹选择对话框打开
- ✅ 选择文件夹后，缩略图显示在中间面板
- ✅ 配置文件创建在 `%APPDATA%\Game Dev Manager\config.json`
- ✅ 配置文件内容：
  ```json
  {
    "last_folder": "C:\\test_sprites"
  }
  ```

**实际结果：** 符合预期  
**通过 / 失败：** ✅ **通过**

---

### 测试用例 3：重启后自动恢复

**目标：** 程序应在下次启动时自动打开上次选择的文件夹。

**步骤：**
1. 确保配置文件存在（从测试用例 2）
2. 启动程序：`python -m gdm.main`
3. 观察测试用例 2 中的文件夹是否自动打开

**预期结果：**
- ✅ 主窗口打开
- ✅ 测试用例 2 中的文件夹自动打开（显示缩略图）
- ✅ 左侧面板显示文件夹路径
- ✅ **无需**手动点击"打开文件夹"

**实际结果：** 符合预期  
**通过 / 失败：** ✅ **通过**

---

### 测试用例 4：无效路径处理

**目标：** 如果 `last_folder` 指向不存在的路径，程序应静默跳过（不崩溃）。

**步骤：**
1. 编辑配置文件：将 `last_folder` 改为不存在的路径
   ```json
   {
       "last_folder": "C:\\non_existent_folder"
   }
   ```
2. 启动程序：`python -m gdm.main`
3. 观察程序是否启动且无错误

**预期结果：**
- ✅ 主窗口成功打开
- ✅ 无错误对话框或控制台错误
- ✅ 程序显示空白窗口（无缩略图，因为文件夹不存在）
- ✅ 无崩溃或冻结

**实际结果：** 符合预期  
**通过 / 失败：** ✅ **通过**

---

### 测试用例 5：配置文件损坏处理

**目标：** 如果配置文件不是有效的 JSON，程序应静默跳过（不崩溃）。

**步骤：**
1. 编辑配置文件：写入无效的 JSON
   ```json
   { invalid json }
   ```
2. 启动程序：`python -m gdm.main`
3. 观察程序是否启动且无错误

**预期结果：**
- ✅ 主窗口成功打开
- ✅ 无错误对话框或控制台错误
- ✅ 程序显示空白窗口
- ✅ 无崩溃或冻结

**实际结果：** 符合预期  
**通过 / 失败：** ✅ **通过**

---

### 测试用例 6：选择不同文件夹更新配置

**目标：** 选择不同的文件夹应更新配置文件中的 `last_folder`。

**步骤：**
1. 启动程序（从测试用例 3，配置包含 `C:\test_sprites`）
2. 点击"文件" → "打开文件夹"
3. 选择一个**不同的**文件夹（例如 `C:\other_sprites`）
4. 关闭程序
5. 检查配置文件内容

**预期结果：**
- ✅ 新文件夹的缩略图在选择后显示
- ✅ 配置文件已更新：`last_folder` 现在指向 `C:\other_sprites`

**实际结果：** 符合预期  
**通过 / 失败：** ✅ **通过**

---

### 测试用例 7：权限错误处理（可选）

**目标：** 如果配置文件无法写入（权限错误），程序不应崩溃。

**步骤：**
1. 设置配置目录为只读权限（Windows）：
   ```bash
   # Windows 上可能不生效，如果无法设置则跳过
   icacls "%APPDATA%\Game Dev Manager" /grant Everyone:R
   ```
2. 启动程序并尝试选择文件夹
3. 观察程序是否优雅地处理错误

**预期结果：**
- ✅ 程序不崩溃
- ✅ 警告记录到控制台（GUI 模式下可能不可见）
- ✅ 程序继续运行（仍可以选择其他文件夹）

**实际结果：** 跳过（Windows 上难以设置权限）  
**通过 / 失败：** 跳过

---

## 发现的问题

无

---

## 建议改进

无

---

## 签署

**测试人员签名：** oliveryin  
**日期：** 2026-06-12  
**结论：** ✅ **功能正常，所有测试通过**
