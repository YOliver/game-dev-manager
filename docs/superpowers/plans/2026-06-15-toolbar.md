# 功能栏（Toolbar）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在菜单栏下方添加 QToolBar，动态显示当前选中菜单的子功能项

**Architecture:** 添加 QToolBar 组件，利用 QMenu.aboutToShow 信号切换工具栏内容，工具栏按钮复用菜单项的 QAction

**Tech Stack:** Python, PySide6, pytest

---

### Task 1: 添加测试 — 验证工具栏创建和默认显示

**Files:**
- Modify: `tests/test_main_window.py`（末尾追加）

- [ ] **Step 1: 写入测试**

新增 `TestToolbarCreation` 测试类，追加到文件末尾：

```python
class TestToolbarCreation:
    """测试功能栏的创建和默认显示。"""

    def test_toolbar_exists_and_is_q_toolbar(
        self, main_window
    ):
        """MainWindow 应包含一个 QToolBar 实例。"""
        from PySide6.QtWidgets import QToolBar

        assert hasattr(main_window, "toolbar")
        assert isinstance(main_window.toolbar, QToolBar)

    def test_toolbar_default_actions(
        self, main_window
    ):
        """默认应显示"文件"菜单的子项。"""
        expected_texts = ["打开文件夹", "保存工作区", "退出"]

        actual_texts = []
        for action in main_window.toolbar.actions():
            actual_texts.append(action.text())

        assert actual_texts == expected_texts
```

- [ ] **Step 2: 运行测试确认失败**

```bash
python -m pytest tests/test_main_window.py::TestToolbarCreation -v
```

预期：FAIL — 因为 toolbar 尚未添加（TDD 正常流程）

- [ ] **Step 3: 提交**

```bash
git add tests/test_main_window.py
git commit -m "test: 新增功能栏创建和默认显示的测试"
```

---

### Task 2: 添加测试 — 验证工具栏随菜单切换

**Files:**
- Modify: `tests/test_main_window.py`（末尾追加）

- [ ] **Step 1: 写入测试**

新增 `TestToolbarUpdate` 测试类，追加到文件末尾：

```python
class TestToolbarUpdate:
    """测试点击菜单后功能栏应更新内容。"""

    def test_toolbar_updates_on_menu_about_to_show(
        self, main_window
    ):
        """点击工具菜单后，工具栏应显示工具菜单的子项。"""
        # 获取菜单栏中的"工具"菜单
        menu_bar = main_window.menuBar()
        tool_menu = None
        for action in menu_bar.actions():
            if action.text() == "工具":
                tool_menu = action.menu()
                break

        assert tool_menu is not None

        # 触发 aboutToShow 信号
        tool_menu.aboutToShow.emit()

        # 验证工具栏已更新为工具菜单的子项
        expected_texts = ["批量重命名"]
        actual_texts = [action.text() for action in main_window.toolbar.actions()]
        assert actual_texts == expected_texts

    def test_toolbar_help_menu_actions(
        self, main_window
    ):
        """点击帮助菜单后，工具栏应显示帮助菜单的子项。"""
        menu_bar = main_window.menuBar()
        help_menu = None
        for action in menu_bar.actions():
            if action.text() == "帮助":
                help_menu = action.menu()
                break

        assert help_menu is not None

        help_menu.aboutToShow.emit()

        expected_texts = ["使用手册", "欢迎", "软件信息"]
        actual_texts = [action.text() for action in main_window.toolbar.actions()]
        assert actual_texts == expected_texts

    def test_toolbar_ignores_separators(
        self, main_window
    ):
        """功能栏中不应包含分隔线。"""
        menu_bar = main_window.menuBar()
        file_menu = None
        for action in menu_bar.actions():
            if action.text() == "文件":
                file_menu = action.menu()
                break

        # 文件菜单有分隔线，但工具栏中应跳过
        for action in main_window.toolbar.actions():
            assert not action.isSeparator(), "工具栏不应包含分隔线"
```

- [ ] **Step 2: 运行测试确认失败**

```bash
python -m pytest tests/test_main_window.py::TestToolbarUpdate -v
```

预期：FAIL — 因为 toolbar 和 aboutToShow 连接尚未实现

- [ ] **Step 3: 提交**

```bash
git add tests/test_main_window.py
git commit -m "test: 新增功能栏随菜单切换的测试"
```

---

### Task 3: 实现功能 — 添加 QToolBar 和 _update_toolbar

**Files:**
- Modify: `gdm/gui/main_window.py`

- [ ] **Step 1: 添加 QToolBar 导入**

在文件顶部第 12 行的 `QMainWindow,` 后添加 `QToolBar,`：

```python
from PySide6.QtWidgets import (
    QApplication,
    QFileDialog,
    QHBoxLayout,
    QMainWindow,
    QSplitter,
    QToolBar,
    QWidget,
)
```

- [ ] **Step 2: 在 `_init_ui()` 中添加 toolbar**

在 `_init_menubar()` 调用之前（第 85 行附近）添加：

```python
        # 功能栏
        self.toolbar = QToolBar("功能栏")
        self.toolbar.setMovable(False)
        self.addToolBar(Qt.TopToolBarArea, self.toolbar)

        # 菜单栏
        self._init_menubar()
```

- [ ] **Step 3: 在 `_init_menubar()` 中连接 aboutToShow 信号**

在每个顶级菜单创建后连接信号，并在方法末尾设置默认内容。

在 `file_menu` 创建后（`file_menu = menubar.addMenu("文件")` 之后）添加：

```python
        file_menu.aboutToShow.connect(lambda: self._update_toolbar(file_menu))
```

在 `tool_menu` 创建后添加：

```python
        tool_menu.aboutToShow.connect(lambda: self._update_toolbar(tool_menu))
```

在 `help_menu` 创建后添加：

```python
        help_menu.aboutToShow.connect(lambda: self._update_toolbar(help_menu))
```

在 `_init_menubar()` 末尾（第 123 行之后）添加默认显示：

```python
        # 默认显示"文件"菜单的功能项
        self._update_toolbar(file_menu)
```

- [ ] **Step 4: 新增 `_update_toolbar()` 方法**

在类末尾（`closeEvent` 方法之后）添加：

```python
    def _update_toolbar(self, menu) -> None:
        """根据菜单更新功能栏内容。"""
        self.toolbar.clear()
        for action in menu.actions():
            if action.isSeparator():
                continue
            self.toolbar.addAction(action)
```

- [ ] **Step 5: 运行测试确认通过**

```bash
python -m pytest tests/test_main_window.py::TestToolbarCreation tests/test_main_window.py::TestToolbarUpdate -v
```

预期：全部 PASS

- [ ] **Step 6: 提交**

```bash
git add gdm/gui/main_window.py
git commit -m "feat: 添加功能栏，动态显示当前菜单的子功能项"
```

---

### Task 4: 运行全量测试并确认

**Files:**
- 无新建/修改

- [ ] **Step 1: 运行全量测试**

```bash
python -m pytest tests/ -v
```

预期：全部 PASS

- [ ] **Step 2: 确认无未提交文件**

```bash
git status
```

---

## 任务依赖顺序

```
Task 1 (test) → Task 2 (test) → Task 3 (impl) → Task 4 (full)
```

## 预期提交记录

```
test: 新增功能栏创建和默认显示的测试
test: 新增功能栏随菜单切换的测试
feat: 添加功能栏，动态显示当前菜单的子功能项
```
