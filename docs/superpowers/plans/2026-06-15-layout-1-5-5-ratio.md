# 界面布局 1:5:5 比例调整实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将主窗口三面板布局的初始比例从 1:3:1 调整为 1:5:5

**Architecture:** 仅修改 QSplitter 的初始大小设置，使用相对比例值让 Qt 自动分配空间

**Tech Stack:** Python, PySide6, pytest

---

### Task 1: 修改布局初始比例

**Files:**
- Modify: `gdm/gui/main_window.py:73-74`

- [ ] **Step 1: 修改 setSizes() 调用**

在 `gdm/gui/main_window.py` 第 73-74 行，将：

```python
# 设置初始大小比例 左侧:中间:右侧 = 200:600:200
splitter.setSizes([200, 600, 200])
```

改为：

```python
# 设置初始大小比例 左侧:中间:右侧 = 1:5:5
splitter.setSizes([1, 5, 5])
```

- [ ] **Step 2: 运行测试确认无回归**

```bash
cd G:/UGit/game-dev-manager && python -m pytest tests/ -v
```

预期输出：所有测试通过（当前 62 passed, 1 skipped）

- [ ] **Step 3: 手动验证布局效果**

启动应用并验证：
1. 启动 GDM：`python -m gdm`
2. 观察初始布局：左侧面板较窄，中间和右侧面板宽度相近
3. 拖动分隔条：确认可自由调整各面板宽度
4. 调整窗口大小：观察面板是否按合理比例缩放

- [ ] **Step 4: 提交修改**

```bash
cd G:/UGit/game-dev-manager
git add gdm/gui/main_window.py
git commit -m "feat: 调整主窗口布局比例为 1:5:5

将三面板初始比例从 1:3:1 (200:600:200) 调整为 1:5:5，
使中间缩略图视图和右侧详情面板更宽，提升视觉平衡
和可用性。使用相对比例值 [1, 5, 5] 让 Qt 自动
分配空间，适应用户窗口大小。

Co-Authored-By: Claude <noreply@anthropic.com>"
```
